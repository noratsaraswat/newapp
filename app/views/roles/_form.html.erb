<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>
      <ul>
      <% @role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Role" %><br />
    <%= f.select(:name,[ [ "Administrators", "admin"],["Content Developers", "deve"],[ "Teacher", "teacher"],["Student", "student"] ] )%>
  </div>
  <div class="field">
    <%= f.label "User" %><br />
    <%= f.select(:user_id, User.find(:all).collect {|c| [ c.name, c.id ] })%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
