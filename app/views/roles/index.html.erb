<table>
  <tr>
    <th>Name</th>
    <th>e-mail</th>
    <th>Role</th>
    <th></th>
    <th></th>
  </tr>

<% @roles.each do | role | %>
  <tr>
     <td><%= role.user.name %></td>
    <td><%= role.user.email %></td>
    <td><%= role.name %></td>
    <td><%= link_to 'Edit', edit_role_path(role) %></td>
    <td><%= link_to 'Destroy', role, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%#= link_to 'New Role', new_role_path %>
<%=link_button_to "Back to Role",url_for(:action=>"index", :controller=>"admin_people") %>
